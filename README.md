To install, paste the following in your terminal:
git clone https://github.com/Sacksquerque/Checkers2.git
cd multiplayer-checkers
npm install
Run the server:

To run, paste the following in your terminal:
node server.js